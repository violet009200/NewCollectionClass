# NewCollectionClass
write a new library for collection classes in C++. These classes act as containers for integer numbers

- Collection class: abstract class, has 2 concrete collection subclasses
- LList : concrete subclass of Collection class. implements a collection as a linked list
- Array: concrete subclass of Collection class. Implements a collection as fixed-length arrays
